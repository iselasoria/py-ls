"""
Consider the set:
numbers = {1, 2, 3, 4, 5, 5, 4, 3}
print(len(numbers))

what is the set's length?
"""

# solution:
# 5 since sets don't allow for duplicate values.
numbers = {1, 2, 3, 4, 5, 5, 4, 3}
print(len(numbers))
